# How to setup

```bash
$ npm init -y
$ npm i net
```

Then add the following key-value pair in top-level of `package.json`:

```json
"type": "module"
```

# How to run

```bash
$ nodemon client.js
```