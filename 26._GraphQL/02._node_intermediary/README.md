# How to setup

```sh
npm init -y
npm i express
npm i express-graphql
npm i graphql
```

Then add the following key-value pair in top-level of `package.json`:

```json
"type": "module"
```

# How to run

```sh
nodemon app.js
```