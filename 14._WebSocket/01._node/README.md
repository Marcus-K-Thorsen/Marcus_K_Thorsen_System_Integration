# How to setup

```bash
$ npm init -y
$ npm i ws
```

Then add the following key-value pair in top-level of `package.json`:

```json
"type": "module"
```

# How to run

```bash
$ nodemon server.js
$ node client.js
```

