# How to setup

```bash
$ npm init -y
$ npm i express
$ npm i cors
```

Then add the following key-value pair in top-level of `package.json`:

```json
"type": "module"
```

# How to run

```bash
$ nodemon app.js
```
